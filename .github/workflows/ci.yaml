name: CI

on:
  pull_request:
  workflow_dispatch:

jobs:
  pre-commit:
    permissions:
      contents: read
    uses: ./.github/workflows/pre-commit.yaml
  test-android:
    permissions:
      contents: read
    uses: ./.github/workflows/test-android-unit.yaml
  test-ios:
    permissions:
      contents: read
    uses: ./.github/workflows/test-ios.yaml
    secrets:
      SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
      FIREBASE_KEY: ${{ secrets.FIREBASE_KEY }}
      GOOGLE_APP_ID_IOS: ${{ secrets.GOOGLE_APP_ID_IOS_STAGING }}
  emulator-test-android:
    permissions:
      contents: read
    uses: ./.github/workflows/test-android-instrumented.yaml
  build-android:
    permissions:
      id-token: write
      contents: read
      pull-requests: write
    uses: ./.github/workflows/build-android.yaml
    with:
      build-type: "Debug"
      flavor: "Staging"
      sentry-environment: "debug"
      upload-artifacts: false
    secrets:
      FIREBASE_KEY: ${{ secrets.FIREBASE_KEY }}
      GOOGLE_APP_ID_ANDROID: ${{ secrets.GOOGLE_APP_ID_ANDROID_STAGING }}
      SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
      SENTRY_DSN_ANDROID: ${{ secrets.SENTRY_DSN_ANDROID }}
